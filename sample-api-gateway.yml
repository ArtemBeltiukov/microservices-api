endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: true

zuul:
  routes:
    workspaces-api: 
      path: /workspaces1/**
      serviceId: workspaces-service
    employees-api: 
      path: /employees1/**
      serviceId: employees-service
