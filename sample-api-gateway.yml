endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: true

zuul:
  routes:
    workspaces-service: 
      path: /workspaces/**
      serviceId: workspaces-service
    employees-api: /employees/**
